name: build
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - uses: harehare/elm-action@v1.0.27
        with:
          working_directory: ./elm1
